{
  "name": "resolve-npm-version",
  "version": "0.0.0",
  "description": "Given a json object from a npm compatible regitry and a version or tag, return the latest compatible version",
  "main": "dist/index.js",
  "scripts": {
    "test": "babel-tape-runner test.js && semistandard | snazzy",
    "build": "rm -rf dist && mkdir -p dist && babel lib --out-dir dist",
    "watch": "rm -rf dist && mkdir -p dist && babel -w lib --out-dir dist",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kesla/resolve-npm-verion.git"
  },
  "author": "David Björklund <david.bjorklund@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/kesla/resolve-npm-verion/issues"
  },
  "homepage": "https://github.com/kesla/resolve-npm-verion#readme",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.10.4",
    "babel-preset-es2015": "^6.9.0",
    "babel-tape-runner": "^2.0.1",
    "semistandard": "^8.0.0",
    "snazzy": "^4.0.0",
    "tapava": "^2.2.0"
  }
}